# Weather Data ETL Pipeline (Scheduled)

Project in progress — building an automated ETL pipeline that extracts weather data from an API on a schedule, transforms it, and loads it into a local SQLite database.